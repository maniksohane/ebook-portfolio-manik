const express=require('express');const {requireUser,requireAdmin}=require('../middleware/auth');const {createEbook,listAdminEbooks,updateEbook,createUploadUrl}=require('../controllers/ebookController');const r=express.Router();r.use(requireUser,requireAdmin);r.get('/ebooks',listAdminEbooks);r.post('/ebooks',createEbook);r.patch('/ebooks/:id',updateEbook);r.post('/uploads/signed-url',createUploadUrl);module.exports=r;
